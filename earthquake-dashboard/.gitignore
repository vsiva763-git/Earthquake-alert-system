# Dependencies (install via npm install)
node_modules/

# Build outputs (generated by npm run build)
/dist

# OS files
.DS_Store
Thumbs.db

# Editor files (optional - can commit if team uses same IDE)
# .vscode/*
# .idea

# Logs (optional - can commit if needed for debugging)
# npm-debug.log*
# yarn-debug.log*
# pnpm-debug.log*

# IMPORTANT: .env IS COMMITTED (not ignored)
# All environment files are included for easy setup
# .env.local
# .env.development.local
# .env.test.local
# .env.production.local

